{
  "version": 1,
  "request_id": "e5555555-5555-4555-8555-202609180002",
  "device_id": "Leno",
  "op": "files.patch",
  "args": {
    "path": "agent/providers/search/src/search_tests.rs",
    "old": "async fn collect_relative_paths(provider: &SearchProvider, handle: &str) -> Vec<String> {\n    let mut paths = Vec::new();\n    for _ in 0..200 {\n        let result = provider\n            .execute(&invocation(\n                \"search.read\",\n                json!({ \"handle\": handle, \"max_items\": 64 }),\n            ))\n            .await\n            .unwrap();\n\n        for item in result.data[\"matches\"].as_array().unwrap() {\n            paths.push(item[\"relative_path\"].as_str().unwrap().to_owned());\n        }\n        if result.data[\"done\"].as_bool() == Some(true) {\n            paths.sort();\n            return paths;\n        }\n        tokio::time::sleep(Duration::from_millis(5)).await;\n    }\n    panic!(\"search did not reach a terminal state\");\n}\n",
    "new": "async fn collect_matches(provider: &SearchProvider, handle: &str) -> Vec<Value> {\n    let mut matches = Vec::new();\n    for _ in 0..200 {\n        let result = provider\n            .execute(&invocation(\n                \"search.read\",\n                json!({ \"handle\": handle, \"max_items\": 64 }),\n            ))\n            .await\n            .unwrap();\n\n        matches.extend(result.data[\"matches\"].as_array().unwrap().iter().cloned());\n        if result.data[\"done\"].as_bool() == Some(true) {\n            return matches;\n        }\n        tokio::time::sleep(Duration::from_millis(5)).await;\n    }\n    panic!(\"search did not reach a terminal state\");\n}\n\nasync fn collect_relative_paths(provider: &SearchProvider, handle: &str) -> Vec<String> {\n    let mut paths: Vec<String> = collect_matches(provider, handle)\n        .await\n        .into_iter()\n        .map(|item| item[\"relative_path\"].as_str().unwrap().to_owned())\n        .collect();\n    paths.sort();\n    paths\n}\n\nasync fn start_content(\n    provider: &SearchProvider,\n    root: &Path,\n    query: &str,\n    regex: bool,\n    case_sensitive: bool,\n    globs: &[&str],\n    context_lines: usize,\n    max_results: usize,\n) -> String {\n    let result = provider\n        .execute(&invocation(\n            \"search.start\",\n            json!({\n                \"root\": root,\n                \"scope\": \"content\",\n                \"query\": query,\n                \"regex\": regex,\n                \"case_sensitive\": case_sensitive,\n                \"include_hidden\": false,\n                \"globs\": globs,\n                \"context_lines\": context_lines,\n                \"max_results\": max_results,\n            }),\n        ))\n        .await\n        .unwrap();\n\n    let handle = result.data[\"handle\"].as_str().unwrap().to_owned();\n    assert!(handle.starts_with(\"search_\"));\n    handle\n}\n",
    "expected_replacements": 1
  }
}
