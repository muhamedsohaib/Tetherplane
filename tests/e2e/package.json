{
  "name": "@tetherplane/e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "pretest": "cargo build -p tetherd && pnpm --filter @tetherplane/compact-mcp build && pnpm --filter @tetherplane/model-client build",
    "build": "tsc -p tsconfig.json",
    "test": "node --test *.test.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "@tetherplane/compact-mcp": "workspace:*",
    "@tetherplane/model-client": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "24.13.6"
  }
}
